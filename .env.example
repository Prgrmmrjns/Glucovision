# Copy to ".env" in the repository root (same folder as README).
# Required only if you regenerate D1NAMO meal macronutrients with Mistral Pixtral
# (food_annotations/food_annotations.ipynb). Not needed to run analysis_scripts/
# when using the committed CSVs under food_data/pixtral-large-latest/.

MISTRAL_API_KEY=
